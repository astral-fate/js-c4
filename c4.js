let DolScore = (96+108+89)/3;
console.log(`the score is ${DolScore}`);


let KolaScore= (88+ 91 + 110)/3

console.log(`the score is ${KolaScore}`);


if (DolScore> KolaScore ){
    console.log(`dolphin score is bigger, it wins, because the score is ${DolScore}`);
}
else if (KolaScore>DolScore  ){
    console.log(`kola score is bigger, it wins, because the score is ${KolaScore}`);
}
else if (DolScore === KolaScore) {
    console.log(`kola score and dolphin scores are equal`);

}